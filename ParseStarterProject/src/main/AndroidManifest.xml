<?xml version="1.0" encoding="utf-8"?>
<!--
  ~ Copyright (c) 2015-present, Parse, LLC.
  ~ All rights reserved.
  ~
  ~ This source code is licensed under the BSD-style license found in the
  ~ LICENSE file in the root directory of this source tree. An additional grant
  ~ of patent rights can be found in the PATENTS file in the same directory.
  -->
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.parse.starter" >

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

    <application
        android:name=".StarterApplication"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:theme="@style/AppTheme" >
        <meta-data
            android:name="com.parse.APPLICATION_ID"
            android:value="@string/parse_app_id" />
        <meta-data
            android:name="com.parse.CLIENT_KEY"
            android:value="@string/parse_client_key" />

        <activity
            android:name=".MainActivity"
            android:label="@string/app_name" >
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".UserList"
            android:label="@string/app_name" >
            <intent-filter>
                <action android:name="com.parse.starter.UserList" />

                <category android:name="android.intent.category.DEFAULT" />
            </intent-filter>

        </activity>

        <activity
            android:name=".UserFeed"
            android:label="@string/app_name" >
            <intent-filter>
                <action android:name="com.parse.starter.UserFeed" />

                <category android:name="android.intent.category.DEFAULT" />
            </intent-filter>

        </activity>

        <activity
            android:name=".Efficency"
            android:label="@string/app_name" >
            <intent-filter>
                <action android:name="com.parse.starter.Efficency" />

                <category android:name="android.intent.category.DEFAULT" />
            </intent-filter>

        </activity>

        <activity
            android:name=".Production"
            android:label="@string/app_name" >
            <intent-filter>
                <action android:name="com.parse.starter.Production" />

                <category android:name="android.intent.category.DEFAULT" />
            </intent-filter>

        </activity>

        <activity
            android:name=".BarGraphs"
            android:label="@string/app_name" >
            <intent-filter>
                <action android:name="com.parse.starter.BarGraphs" />

                <category android:name="android.intent.category.DEFAULT" />
            </intent-filter>

        </activity>

        <activity
            android:name=".PieCharts"
            android:label="@string/app_name" >
            <intent-filter>
                <action android:name="com.parse.starter.PieCharts" />

                <category android:name="android.intent.category.DEFAULT" />
            </intent-filter>

        </activity>

        <activity
            android:name=".SplashScreen"
            android:label="@string/app_name" >
            <intent-filter>
                <action android:name="com.parse.starter.SplashScreen" />

                <category android:name="android.intent.category.DEFAULT" />
            </intent-filter>

        </activity>

        <activity
            android:name=".DataNotFetched"
            android:label="@string/app_name" >
            <intent-filter>
                <action android:name="com.parse.starter.DataNotFetched" />

                <category android:name="android.intent.category.DEFAULT" />
            </intent-filter>

        </activity>

        <activity
            android:name=".LineGraph"
            android:label="@string/app_name" >
            <intent-filter>
                <action android:name="com.parse.starter.LineGraph" />

                <category android:name="android.intent.category.DEFAULT" />
            </intent-filter>

        </activity>

        <activity
            android:name=".CombinedChart"
            android:label="@string/app_name" >
            <intent-filter>
                <action android:name="com.parse.starter.CombinedChart" />

                <category android:name="android.intent.category.DEFAULT" />
            </intent-filter>

        </activity>


        <activity
            android:name=".EfficiencyGraph"
            android:label="@string/app_name" >
            <intent-filter>
                <action android:name="com.parse.starter.EfficiencyGraph" />

                <category android:name="android.intent.category.DEFAULT" />
            </intent-filter>

        </activity>
    </application>

</manifest>
